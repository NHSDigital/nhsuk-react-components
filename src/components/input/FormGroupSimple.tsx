import React from "react";
import { FormGroup, Input } from "nhsuk-react-components";


const FormGroupSimple = (props, this.optionalLink) => (
    <FormGroup {...props}>
        <Input hint={hint} label={label} />
        <a href=""></a>
    </FormGroup>
);

export default FormGroupSimple;